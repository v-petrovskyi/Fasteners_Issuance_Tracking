<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QRAndBarcodeScanner" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/my_light_primary1</item>
    </style>

    <style name="Theme.QRAndBarcodeScanner" parent="Base.Theme.QRAndBarcodeScanner" />

    <style name="DialogTheme" parent="Theme.QRAndBarcodeScanner">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowIsFloating">false</item>
    </style>
</resources>